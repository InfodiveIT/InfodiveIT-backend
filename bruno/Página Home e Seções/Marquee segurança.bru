meta {
  name: Marquee segurança
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/home-seguranca-marquee
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080/api/v1
}

docs {
  PÚBLICO. Retorna os dados dos cards dinâmicos do marquee (painel de segurança animado) da home.
}
