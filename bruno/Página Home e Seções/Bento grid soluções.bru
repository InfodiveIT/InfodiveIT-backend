meta {
  name: Bento grid soluções
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/home-solucoes-bento
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080/api/v1
}

docs {
  PÚBLICO. Retorna a lista de itens configuráveis para o grid do tipo Bento das soluções na home.
}
