meta {
  name: CTAs
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/ctas/solucoes
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080/api/v1
}

docs {
  PÚBLICO. Retorna as configurações de Call To Action (CTA) de uma página específica.
  Opções de páginas: `solucoes`, `produtos`, `servicos`, `sobre`, `blog_artigo`.
}
