meta {
  name: Banners Hero
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/paginas-hero/home
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8080/api/v1
}

docs {
  PÚBLICO. Retorna as configurações de Hero Banner de uma página específica.
  Opções de páginas: `home`, `solucoes`, `produtos`, `servicos`, `sobre`, `blog`.
}
